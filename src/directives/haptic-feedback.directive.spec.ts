import { HapticFeedbackDirective } from './haptic-feedback.directive';

describe('HapticFeedbackDirective', () => {
  it('should create an instance', () => {
    const directive = new HapticFeedbackDirective();
    expect(directive).toBeTruthy();
  });
});
